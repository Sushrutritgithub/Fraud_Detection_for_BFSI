This directory should contain trained model artifacts and preprocessing pipelines.

Expected files (examples):
- `rf_fraud_model.joblib`
- `xgboost_model.bin`
- `preprocessing_pipeline.pkl`

Do not commit large binary model files into source control; use an artifacts store instead.
